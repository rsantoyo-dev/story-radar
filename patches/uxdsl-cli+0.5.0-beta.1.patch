diff --git a/node_modules/uxdsl-cli/bin/uxdsl.js b/node_modules/uxdsl-cli/bin/uxdsl.js
index e283b4e..e73969b 100755
--- a/node_modules/uxdsl-cli/bin/uxdsl.js
+++ b/node_modules/uxdsl-cli/bin/uxdsl.js
@@ -180,6 +180,8 @@ async function loadConfig(argv) {
     resolvedConfig.breakpoints = configModule.breakpoints || DEFAULT_BREAKPOINTS;
     resolvedConfig.watch = configModule.watch || [];
     resolvedConfig.theme = configModule.theme;
+    resolvedConfig.includeTheme = configModule.includeTheme;
+    resolvedConfig.references = configModule.references;
   }
   
   // Final normalization
@@ -229,7 +231,9 @@ async function buildOnce(config) {
     postcssAdvancedVariables(),
     uxdslPlugin({ 
       breakpoints: config.breakpoints || DEFAULT_BREAKPOINTS,
-      theme: config.theme
+      theme: config.theme,
+      includeTheme: config.includeTheme,
+      references: config.references,
     }),
   ]).process(source, {
     from: config.entry,
